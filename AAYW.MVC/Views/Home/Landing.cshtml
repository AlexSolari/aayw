@{ 
    ViewBag.Title = "Welcome";
}
<div class="landing mui-panel center">
    <img src="~/Content/img/logo-big.png" alt="@Html.Text("LandingWelcome")" />
    <h1>
        @Html.Text("LandingWelcome")
    </h1>
    <p>
        @Html.Text("LandingExplanation")
    </p>
    <a href="@Url.RouteUrl("Home")" class="mui-btn mui-btn--flat">@Html.Text("Home")</a>
</div>
